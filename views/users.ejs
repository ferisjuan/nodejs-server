<%- include('./includes/head.ejs') %>

<body>
<% if (users.length > 0) { %>
<% for(let item of users) {%> 
  <ul>
      <li><%= item.user %></li>
  </ul>
  <% } %> 
<% } else { %>
  <h1>No users yet</h1>
<% } %> 
<%- include('./includes/end.ejs') %>